services:
  app:
    build: .
    container_name: atomic-flow-app
    restart: always
    ports:
      - "${SERVER_PORT:-9501}:${SERVER_PORT:-9501}"
    environment:
      - APP_ENV=production
    networks:
      - atomic-network

networks:
  atomic-network:
    driver: bridge

